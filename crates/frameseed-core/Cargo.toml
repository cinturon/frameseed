[package]
name = "frameseed-core"
version = "0.1.0"
edition = "2024"

[dependencies]
image = "0.25.5"
rand = "0.9"
rand_chacha = "0.9"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
rayon = "1.10"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "scene_render"
harness = false